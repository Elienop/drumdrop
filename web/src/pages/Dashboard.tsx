// Placeholder shell. The real Dashboard (summary cards, live downloads, recent
// jobs, sync controls) is implemented in Task 15.
export function Dashboard() {
  return <h1 className="text-2xl font-bold">Dashboard</h1>
}
