node_modules/
downloads/
*.log
.env*
.secrets*
*.har
docs/superpowers/
.superpowers/

# Go
/drumdrop
/dist/
*.test

# At-rest state files, in case DRUMDROP_CONFIG_DIR points into the repo
*.db
*.db-wal
*.db-shm
secret.key
credentials.enc
session.cookie

# Server runtime artifacts
*.pid
*.sock

# Built web UI assets
/web/dist/

# Claude Code's local folder: its settings, agent worktrees, and the CLAUDE.md
# symlink into the owner's Obsidian vault (the project rules note). Kept local on
# purpose, like docs/superpowers/ above: none of it belongs in the repo's history.
.claude/
